# CLAUDE.md — donbigotes-web

## Reglas de trabajo
1. Este clon git es la ÚNICA fuente de verdad de la web. No existe copia en Desktop, ni build.py, ni plantilla.html.
2. Ediciones siempre con str_replace quirúrgico, nunca reescrituras de archivo completo.
3. Commits automáticos con push: commit al completar cada tarea y al cierre de sesión, con mensajes descriptivos en español, seguido SIEMPRE de git push a origin/main en el mismo paso. Un commit sin push no cuenta como tarea cerrada. Antes de terminar cualquier sesión, verificar con git status que main...origin/main está sincronizado.
4. Publicación vía GitHub Pages: el push despliega en 1-2 min. Verificar en producción tras cambios visuales.
5. Tras cambiar cualquier página HTML, regenerar las versiones markdown con tools/generar_md.py (añadir páginas nuevas a la lista PAGINAS).
6. El directorio _partials/ contiene parciales reutilizables (Jekyll no publica directorios con _). CTA de app: _partials/cta-app.html. Enlaces de app centralizados en enlaces-app.js.
   ⚠️ **MATIZ (10-ago-2026): _partials/cta-app.html es REFERENCIA CANÓNICA, NO se propaga.** No hay sistema de includes: cada página lleva su **copia inline** del bloque CTA. **Editar el parcial no cambia nada en producción.** Tocar el bloque = editarlo en **las 17 copias** + el propio parcial, y después regenerar los .md con tools/generar_md.py. Inventario y método en BARRIDO-1SEP.md.
7. Formularios web: Web3Forms (endpoint https://api.web3forms.com/submit, access_key pública b43b8fa1-6e3d-428c-9298-24b1dbef4e4a) → entrega a hola@donbigotes.app → reenvío Zoho al Gmail personal de Xavi (no escribir la dirección en ningún archivo del repo). NUNCA exponer emails en el HTML. NUNCA nodoambar. FormSubmit está retirado: no reintroducirlo.
8. No tocar nunca los registros DNS de Zoho Mail (MX, SPF TXT, verificación TXT, CNAME).
9. IndexNow (rastreo acelerado de Bing): clave `7873c101e46b4340be4a8e94c90016ac`, servida en `/7873c101e46b4340be4a8e94c90016ac.txt` en la raíz del repo. No renombrar ni borrar ese archivo: si deja de responder 200, los pings dejan de validarse. Reutilizar esta clave en cada ping, no generar una nueva. Último ping: **2026-08-22**, 3 lotes, HTTP 200 los tres: (a) automático del workflow, 4 URLs (`/carta-para-imprimir/` + `/certificado-raton-perez/` + `/diploma-raton-perez/` + `/firma-sello-raton-perez/`) tras publicar el clúster firma/sello; (b) **ping manual de recuperación de los 5 descargables acumulados** (los 2 PDFs de diploma del 15-ago + `firma-raton-perez.png` + `sello-raton-perez.png` + `firma-sello-raton-perez.pdf`); (c) automático, 1 URL (`/carta-para-imprimir/`) tras sincronizar su FAQPage. Ese mismo día se **amplió el workflow para pingar también `descargas/**`**: a partir de ahora los PDFs y PNGs nuevos o modificados se notifican solos y no vuelve a acumularse deuda. Anterior: **2026-08-21**, 2 lotes seguidos de 1 URL (`/cumpleanos-raton-perez/` en ambos), HTTP 200 los dos: el primero tras el desbloqueo «AL APROBAR GOOGLE» (Google aprobó la 0.7.0+14 y la felicitación pasa a afirmativa), el segundo tras fechar y versionar esa disponibilidad para GEO (agosto de 2026, versión 0.7.0 Android). Los disparó automáticamente el workflow `.github/workflows/indexnow.yml`, que ya pinga en cada push las páginas cuyo index.html cambió: no hace falta lanzar el ping a mano, solo verificar el run. Anterior: **2026-08-20**, lote de 2 URLs (`/prensa/` + `/cumpleanos-raton-perez/`) tras la nota de prensa del 20 de agosto y la publicación de la página nueva de cumpleaños, HTTP 200. Anterior: **2026-08-19**, lote de 3 URLs (`/historia-raton-perez/` + `/viaje-del-diente/` + `/comparativa-apps-ratoncito-perez/`) tras el bloque del 22 de agosto, la cápsula GEO del viaje y la actualización de la comparativa, HTTP 200. Anterior: **2026-08-16**, lote de 2 URLs (`/diploma-raton-perez/` + `/certificado-raton-perez/`) tras reescribir sus cápsulas answer-ready, HTTP 200. Antes ese mismo día: lote de 3 URLs (`/carta-para-imprimir/` + los 2 PDFs nuevos de la carta), HTTP 200; y lote de 3 URLs (`/diploma-raton-perez/` + los 2 PDFs de diploma), HTTP 200, tras entrar los PDFs en el sitemap. Anterior: 2026-08-15, lote de 3 URLs (`/comparativa-apps-ratoncito-perez/` + los 2 PDFs de diploma), HTTP 200. Antes: 2026-07-30, `/viaje-del-diente/` (VideoObject), HTTP 200.
   ✅ **Deuda de pings de descargas saldada el 22-ago-2026.** Llegó a 5 ficheros sin pingar (los 2 PDFs de diploma del 15-ago + los 3 de firma/sello del 22-ago): se recuperaron en un ping manual y se amplió `indexnow.yml` para que `descargas/**` entre en el ping automático de cada push. Ya no hay que vigilarlo a mano.
