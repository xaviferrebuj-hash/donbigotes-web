# CLAUDE.md — donbigotes-web

**Ítems con disparador (fechas, hitos): `PENDIENTES.md`.** Hoy solo hay uno, [W1], el barrido del copy «gratis» del 1-sep-2026.

## Reglas de trabajo
1. Este clon git es la ÚNICA fuente de verdad de la web. No existe copia en Desktop, ni build.py, ni plantilla.html.
2. Ediciones siempre con str_replace quirúrgico, nunca reescrituras de archivo completo.
3. Commits automáticos con push: commit al completar cada tarea y al cierre de sesión, con mensajes descriptivos en español, seguido SIEMPRE de git push a origin/main en el mismo paso. Un commit sin push no cuenta como tarea cerrada. Antes de terminar cualquier sesión, verificar con git status que main...origin/main está sincronizado.
4. Publicación vía GitHub Pages: el push despliega en 1-2 min. Verificar en producción tras cambios visuales.
5. Tras cambiar cualquier página HTML, regenerar las versiones markdown con tools/generar_md.py (añadir páginas nuevas a la lista PAGINAS).
6. El directorio _partials/ contiene parciales reutilizables (Jekyll no publica directorios con _). CTA de app: _partials/cta-app.html. Enlaces de app centralizados en enlaces-app.js.
   ⚠️ **MATIZ (10-ago-2026): _partials/cta-app.html es REFERENCIA CANÓNICA, NO se propaga.** No hay sistema de includes: cada página lleva su **copia inline** del bloque CTA. **Editar el parcial no cambia nada en producción.** Tocar el bloque = editarlo en **las 17 copias** + el propio parcial, y después regenerar los .md con tools/generar_md.py. Inventario y método en BARRIDO-1SEP.md.
7. Formularios web: Web3Forms (endpoint https://api.web3forms.com/submit, access_key pública b43b8fa1-6e3d-428c-9298-24b1dbef4e4a) → entrega a hola@donbigotes.app → reenvío Zoho al Gmail personal de Xavi (no escribir la dirección en ningún archivo del repo). NUNCA exponer emails en el HTML. NUNCA nodoambar. FormSubmit está retirado: no reintroducirlo.
8. No tocar nunca los registros DNS de Zoho Mail (MX, SPF TXT, verificación TXT, CNAME).
9. IndexNow (rastreo acelerado de Bing): clave `7873c101e46b4340be4a8e94c90016ac`, servida en `/7873c101e46b4340be4a8e94c90016ac.txt` en la raíz del repo. No renombrar ni borrar ese archivo: si deja de responder 200, los pings dejan de validarse. Reutilizar esta clave en cada ping, no generar una nueva. Último ping: **2026-09-05**, 1 lote automático del workflow, run **`33968572312`**, 5 URLs (`/` + `/app-raton-perez/` + `/carta-reyes-magos/` + `/cumpleanos-raton-perez/` + `/prensa/`), HTTP 200, tras el bloque de funciones y precios de la app 0.8.0 (hasta seis peques, felicitación de cumpleaños gratis, sección «Qué es gratis y qué se paga», retirada de «gratis durante el lanzamiento» en prensa). Anterior: **2026-09-02**, 6 lotes automáticos del workflow, HTTP 200 los seis: (f) run **`33677346135`**, 1 URL (`/`), tras colorear el enlace del bloque «Próximamente» de la home; (e) run **`33677298602`**, 8 URLs (`/` + `/app-raton-perez/` + `/carta-reyes-magos/` + `/diario-dientes-de-leche/` + `/diploma-raton-perez/` + `/primer-diente/` + `/ultimo-diente/` + `/voz-raton-perez/`), tras publicar las dos páginas nuevas `/carta-reyes-magos/` y `/diario-dientes-de-leche/` (copy del 2-sep), sus enlaces internos y el bloque «Próximamente: el Cartero Real» de la home; (d) run **`33670463063`**, 1 URL (`/diploma-raton-perez/`), tras precisar en description y og:description que el PDF gratis se rellena a mano; (c) run **`33669205173`**, 4 URLs (`/firma-sello-raton-perez/` + `/certificado-raton-perez/` + `/diploma-raton-perez/` + `/el-ratoncito-perez-existe/`), tras sustituir title, description, og:title y og:description de las 4 páginas con impresiones y sin clics en GSC (5-ago→1-sep); (a) run **`33574934924`**, 20 URLs (todas las páginas del sitemap menos `/blog/`), tras el CTA final de la home con badges Play/App Store, la retirada de #opiniones en `/app-raton-perez/`, la FAQ Android/iPhone de app/voz/foto/vídeo y la sincronización de fechas; (b) run **`33574987414`**, 15 URLs, tras corregir las fechas que el primer pase había desplazado a 2-sep. Anterior: **2026-08-26**, 6 lotes, HTTP 200 los seis: (f) **3 ficheros de `descargas/`** (`sello-raton-perez.png` + `firma-raton-perez.png` + `firma-sello-raton-perez.pdf`), run **`32960323674`**, tras integrar la marca «donbigotes.app» en el sello y la firma. ✅ **Primera vez que se ejercita la rama `descargas/**` del workflow: funciona.** (e) 1 URL (`/comparativa-apps-ratoncito-perez/`), run **`32959566108`**, tras acentuar las `description` de los competidores del `ItemList` (solo ortografía: datos y afirmaciones intactos). (d) **19 URLs — el sitio entero**, run **`32958946574`**, tras acentuar las 19 `description` del JSON-LD `SoftwareApplication` (`Aplicación`, `Ratón Pérez`, `Mágico`, `valentía`, `vídeo`). Es el primer ping masivo: el workflow lo hizo solo porque el `index.html` de las 19 cambió. (c) 2 URLs (`/el-ratoncito-perez-existe/` + `/ultimo-diente/`), run **`32958261724`**, tras acentuar también las 9 respuestas visibles de esas dos páginas (quedan a 0 palabras visibles sin acentuar) y replicarlas en su `FAQPage`; (b) 2 URLs (`/el-ratoncito-perez-existe/` + `/ultimo-diente/`), run **`32957527224`**, tras corregir las 9 preguntas visibles que estaban sin tildes ni «¿» en esas dos páginas y replicarlas en su `FAQPage`; (a) 3 URLs (`/` + `/el-ratoncito-perez-existe/` + `/primer-diente/`), HTTP 200, tras convertir el JSON-LD `FAQPage` en espejo verbatim del texto visible en las 18 páginas (15 ya coincidían; en `index.html` se eliminó la pregunta «Que deja el Raton Perez?», que no existía en el HTML visible). Lo disparó el workflow: **run `32956548692`**. En ese mismo push se corrigió `indexnow.yml`: el patrón de descargas pasa de `^descargas/[^/]+$` a `^descargas/.+$` (+ exclusión de `_` y `.`), así que ya cubre subdirectorios. Anterior: **2026-08-25**, 1 lote de 5 URLs (`/` + `/editable/` + `/el-ratoncito-perez-existe/` + `/primer-diente/` + `/ultimo-diente/`), HTTP 200, tras acotar a Android el periodo gratuito en los puntos donde un usuario de iOS actúa (hints del formulario iOS, `form-ok`, `aria-label` del badge y bloque `#descarga`). Lo disparó el workflow: **run `32898658649`**. El push anterior de ese mismo día (`REDACCIONES-1SEP.md`) no pingó nada y el run salió limpio: solo tocaba un `.md` de raíz, que el workflow no mapea a ninguna URL. `llms.txt` cambió en el mismo push y **tampoco entra en el ping automático** (el workflow solo mapea `index.html` y `descargas/**`): si algún día se quiere notificar, hay que pingarlo a mano. Anterior: **2026-08-22**, 3 lotes, HTTP 200 los tres: (a) automático del workflow, 4 URLs (`/carta-para-imprimir/` + `/certificado-raton-perez/` + `/diploma-raton-perez/` + `/firma-sello-raton-perez/`) tras publicar el clúster firma/sello; (b) **ping manual de recuperación de los 5 descargables acumulados** (los 2 PDFs de diploma del 15-ago + `firma-raton-perez.png` + `sello-raton-perez.png` + `firma-sello-raton-perez.pdf`); (c) automático, 1 URL (`/carta-para-imprimir/`) tras sincronizar su FAQPage. Ese mismo día se **amplió el workflow para pingar también `descargas/**`**: a partir de ahora los PDFs y PNGs nuevos o modificados se notifican solos y no vuelve a acumularse deuda. Anterior: **2026-08-21**, 2 lotes seguidos de 1 URL (`/cumpleanos-raton-perez/` en ambos), HTTP 200 los dos: el primero tras el desbloqueo «AL APROBAR GOOGLE» (Google aprobó la 0.7.0+14 y la felicitación pasa a afirmativa), el segundo tras fechar y versionar esa disponibilidad para GEO (agosto de 2026, versión 0.7.0 Android). Los disparó automáticamente el workflow `.github/workflows/indexnow.yml`, que ya pinga en cada push las páginas cuyo index.html cambió: no hace falta lanzar el ping a mano, solo verificar el run. Anterior: **2026-08-20**, lote de 2 URLs (`/prensa/` + `/cumpleanos-raton-perez/`) tras la nota de prensa del 20 de agosto y la publicación de la página nueva de cumpleaños, HTTP 200. Anterior: **2026-08-19**, lote de 3 URLs (`/historia-raton-perez/` + `/viaje-del-diente/` + `/comparativa-apps-ratoncito-perez/`) tras el bloque del 22 de agosto, la cápsula GEO del viaje y la actualización de la comparativa, HTTP 200. Anterior: **2026-08-16**, lote de 2 URLs (`/diploma-raton-perez/` + `/certificado-raton-perez/`) tras reescribir sus cápsulas answer-ready, HTTP 200. Antes ese mismo día: lote de 3 URLs (`/carta-para-imprimir/` + los 2 PDFs nuevos de la carta), HTTP 200; y lote de 3 URLs (`/diploma-raton-perez/` + los 2 PDFs de diploma), HTTP 200, tras entrar los PDFs en el sitemap. Anterior: 2026-08-15, lote de 3 URLs (`/comparativa-apps-ratoncito-perez/` + los 2 PDFs de diploma), HTTP 200. Antes: 2026-07-30, `/viaje-del-diente/` (VideoObject), HTTP 200.
   ✅ **Deuda de pings de descargas saldada el 22-ago-2026.** Llegó a 5 ficheros sin pingar (los 2 PDFs de diploma del 15-ago + los 3 de firma/sello del 22-ago): se recuperaron en un ping manual y se amplió `indexnow.yml` para que `descargas/**` entre en el ping automático de cada push. Ya no hay que vigilarlo a mano.
10. Antes de tocar el repo, comprobar que no hay otro chat de Code abierto sobre él. Si el push falla por commits ajenos, rebase y reportar el hash ajeno.

## Fechas de página
Tres señales de frescura por página, siempre iguales entre sí: `<p class="fecha-actualizacion">`, `dateModified` del JSON-LD y `<lastmod>` del sitemap. Se calculan con la fecha del último commit git de cada `index.html`. Procedimiento exacto tras cualquier edición de contenido:
1. Commitear el contenido.
2. `bash tools/actualiza-fechas.sh` (lee el sitemap y sincroniza las tres señales).
3. Regenerar los .md: `python3 tools/generar_md.py`.
4. Commit con `[fechas]` en el mensaje. El script ignora los commits con ese marcador al calcular fechas: sin él, la siguiente ejecución tomaría el commit de fechas como edición y desplazaría todas las páginas.
El directorio de scripts es `tools/` (no `tool/`).
